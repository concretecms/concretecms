{
  "$schema": "https://json-schema.org/draft-07/schema",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "skipReason": {
      "$comment": "Use this option to document why testing this attribute is skipped during import/export tests.",
      "type": "string",
      "minLength": 1
    },
    "skipSilent": {
      "$comment": "Set to true to silently skip testing this attribute during import/export tests.",
      "type": "boolean"
    },
    "keepXmlElementsOrder": {
      "$comment": "Set to true to avoid sorting automatically the XML elements.",
      "type": "boolean"
    },
    "richTexts": {
      "$comment": "Use this option to check if a value contains rich text elements and the number of such elements. The keys are the SQL queries used to fetch the values (they should return just one row with just one field). Use the :avID parameter to filter the results by the ID of the attribute value.",
      "type": "object",
      "propertyNames": {
        "pattern": "^(?i:SELECT\\s[^;]+)$"
      },
      "additionalProperties": {
        "type": "object",
        "properties": {
          "numFiles": {
            "$comment": "The number of file links in the rich text.",
            "type": "integer",
            "minimum": 0
          },
          "numImages": {
            "$comment": "The number of image links in the rich text.",
            "type": "integer",
            "minimum": 0
          },
          "numPages": {
            "$comment": "The number of page links in the rich text.",
            "type": "integer",
            "minimum": 0
          }
        },
        "required": [],
        "additionalProperties": false
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
