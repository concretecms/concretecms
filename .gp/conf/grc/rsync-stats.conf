# all words
regexp=\b[^\d\W]+\b
colours="\033[38;5;35m"
-
# all numbers
regexp=\d+
colours=bold green
-
# bytes,seconds, bytes/sec
regexp=bytes|sec|onds
colours=magenta
-
# M or K if preceded by a number (lookbehind)
regexp=(?<=[0-9])(M|K)
colours=bold cyan